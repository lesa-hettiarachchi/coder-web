{
  "name": "coder-web-lambda",
  "version": "1.0.0",
  "description": "Lambda functions for Coder Web application",
  "main": "leaderboard-processor.js",
  "dependencies": {
    "aws-sdk": "^2.1490.0"
  },
  "scripts": {
    "deploy": "zip -r leaderboard-processor.zip . && aws lambda update-function-code --function-name coder-web-leaderboard --zip-file fileb://leaderboard-processor.zip"
  }
}

